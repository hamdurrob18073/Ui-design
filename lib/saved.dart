import 'package:flutter/material.dart';
class saved extends StatelessWidget {
  const saved({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: 
      Center(child:Text("chowdhury"))
    );
  }
}