import 'package:flutter/material.dart';
class record extends StatelessWidget {
  const record({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: 
      Center(child:Text("Hamdu"))
    );
  }
}